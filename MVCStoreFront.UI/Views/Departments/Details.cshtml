@model MVCStoreFront.DATA.EF.Department

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="breadcrumb_part">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="breadcrumb_iner">
                    <h2>Department Details</h2>
                </div>
            </div>
        </div>
    </div>
</section>

<div class="container col-md-12">

    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.DeptName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DeptName)
        </dd>

    </dl>

    @*<p>
            @Html.ActionLink("Edit", "Edit", new { id = Model.DeptID }) |
            @Html.ActionLink("Back to List", "Index")
        </p>*@
    <p>
        @*<a href="@Url.Action("Edit", "Departments", new { id = Model.DeptID, @class = "btn btn-success"})">Edit</a>*@
        @*@Html.ActionLink("Edit", "Edit", new { id = Model.Id })*@
    </p>
    <p class="text-center">
        @*@Html.ActionLink("Back to List", "Index")*@
        <a href="@Url.Action("Index", "Departments")" class="btn btn-dark p-3">Back to Departments List</a>
    </p>
</div>
