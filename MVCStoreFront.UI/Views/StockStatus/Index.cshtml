@model IEnumerable<MVCStoreFront.DATA.EF.StockStatus>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<section class="breadcrumb_part">
    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="breadcrumb_iner">
                    <h2>Stock Statuses</h2>
                </div>
            </div>
        </div>
    </div>
</section>

<div class="container col-md-12">
    @if (Request.IsAuthenticated && User.IsInRole("Admin"))
    {
        <p class="text-center p-5">
            @*@Html.ActionLink("Create New", "Create")
                @Url.Action("Create New", "Create")*@
            <a href="@Url.Action("Create", "StockStatus")" class="btn_3 pull-right">Add New Stock Status</a>
        </p>
    }
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td>
                    @*@Html.ActionLink("Details", "Details", new { id = item.StockStatusID })*@
                    @if (Request.IsAuthenticated && User.IsInRole("Admin"))
                    {
                        @*@:|*@
                        @*@Html.ActionLink("Edit", "Edit", new { id = item.StockStatusID }) @:|*@
                        @Html.ActionLink("Delete", "Delete", new { id = item.StockStatusID })
                    }


                </td>
            </tr>
        }

    </table>
</div>
